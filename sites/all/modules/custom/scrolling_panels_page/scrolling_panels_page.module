<?php

/**
* Implementation of hook_ctools_plugin_directory().
*/
function scrolling_panels_page_ctools_plugin_directory($module, $plugin) {
  if ($module == 'page_manager' || $module == 'panels') {
    return 'plugins/' . $plugin;
  }
}